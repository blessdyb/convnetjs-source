const Vol = (width, height, depth, c) => {
    if (Array.isArray(width)) {

    } else {

    }
};

Vol.prototype = {
    get: (width, height, depth) => {
        
    }
}

module.exports = {
    Vol
};